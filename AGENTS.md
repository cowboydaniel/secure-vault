# Agent Instructions

- Follow PEP 8 style for Python files in this repository.
- Prefer using the provided `run_tests.sh` script before submitting changes unless the change is documentation-only.
- When modifying GUI components under `LINUX_GUI/`, capture a screenshot of the new behavior if feasible.
- Document any new dependencies in `requirements.txt` and justify their use in commit messages.
